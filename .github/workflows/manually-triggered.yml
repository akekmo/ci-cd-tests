name: Manually Triggered

on:
  workflow_dispatch:
    inputs:
      string:
          description: "string input"
          type: string
          required: true
          default: "Default Value"

jobs:
    log-info:
        runs-on: ubuntu-latest
        steps:
            - name : Echo infos
              run: |
               echo "hello world"